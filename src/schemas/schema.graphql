### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Account {
  balance: Float
  id: Int
  user: User
}

input Date {
  day: Int
  month: Int
  year: Int
}

type Mutation {
  createTransaction(creditedAccountUsername: String!, debitedAccountId: Int!, token: String!, value: Float!): Transaction
  createUser(apiKey: String!, password: String!, username: String!): User
}

type Query {
  authenticateUser(apiKey: String!, password: String!, username: String!): Token
  getAccount(token: String!, userId: Int!): Account
  getTransactions(
    accountId: Int!
    filterBy: String = "todos"

    """Data de partida da consulta"""
    fromDate: Date

    """Data de limite da consulta"""
    toDate: Date
    token: String!
  ): [Transaction]
}

type Token {
  token: String
}

type Transaction {
  createdAt: String
  creditedAccount: Account
  debitedAccount: Account
  id: Int
  value: Float
}

type User {
  id: Int
  username: String
}